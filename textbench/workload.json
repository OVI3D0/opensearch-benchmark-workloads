{
  "version": 2,
  "description": "TextBench OTel-logs workload (1B docs) for full-text search + analytical benchmarking. Supports ingest from the CloudFront-hosted corpus (datafusion-ingest / lucene-ingest procedures) AND search-only redline load (redline-ppl-random-mix). Ingest target index is 'textbench-1b' so it matches the 'textbench-*' index pattern the correctness harness queries.",
  "indices": [
    {
      "name": "{{ index_name | default('textbench-1b') }}",
      "body": "{{ index_body | default('index-datafusion.json') }}"
    }
  ],
  "corpora": [
    {
      "name": "textbench",
      "base-url": "https://dbyiw3u3rf9yr.cloudfront.net/corpora/textbench",
      "documents": [
        {
          "source-file": "documents.json.bz2",
          "document-count": {{ document_count | default(1000000000) }}
        }
      ]
    }
  ],
  "operations": [
    {{ benchmark.collect(parts="operations/*.json") }}
  ],
  "test_procedures": [
    {{ benchmark.collect(parts="test_procedures/*.json") }}
  ]
}
